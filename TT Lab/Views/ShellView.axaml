<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TT_Lab.Views"
        xmlns:viewmodels="clr-namespace:TT_Lab.ViewModels"
        xmlns:Project="clr-namespace:TT_Lab.Project"
        xmlns:Triggers="clr-namespace:TT_Lab.Triggers"
        xmlns:controls="clr-namespace:TT_Lab.Controls"
        xmlns:gif="clr-namespace:Avalonia.Labs.Gif;assembly=Avalonia.Labs.Gif"
        xmlns:cal="http://caliburnmicro.com"
        xmlns:controls1="clr-namespace:Classic.Avalonia.Theme.Controls;assembly=Classic.Avalonia.Theme"
        xmlns:avaloniaEdit="https://github.com/avaloniaui/avaloniaedit"
        x:Name="MainApplication"
        mc:Ignorable="d"
        x:Class="TT_Lab.Views.ShellView"
        x:DataType="viewmodels:ShellViewModel"
        cal:Bind.AtDesignTime="True"
        Title="{Binding WindowTitle}" Height="720" Width="1280" MinWidth="854" MinHeight="480">
    <Window.Styles>
        <!-- <Style x:Key="MainCenterTabStyle" Selector="TabControl" Parent="{x:Type TabControl}"> -->
        <!--     <Setter Property="Padding" Value="2"/> -->
        <!--     <Setter Property="HorizontalContentAlignment" Value="Center"/> -->
        <!--     <Setter Property="VerticalContentAlignment" Value="Center"/> -->
        <!--     <Setter Property="Template"> -->
        <!--         <Setter.Value> -->
        <!--             <ControlTemplate TargetType="{x:Type TabControl}"> -->
        <!--                 <Grid x:Name="templateRoot" ClipToBounds="true" KeyboardNavigation.TabNavigation="Local"> -->
        <!--                     <Grid.ColumnDefinitions> -->
        <!--                         <ColumnDefinition/> -->
        <!--                         <ColumnDefinition Width="0"/> -->
        <!--                     </Grid.ColumnDefinitions> -->
        <!--                     <Grid.RowDefinitions> -->
        <!--                         <RowDefinition Height="Auto"/> -->
        <!--                         <RowDefinition Height="*"/> -->
        <!--                     </Grid.RowDefinitions> -->
        <!--                     <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" Height="Auto" Grid.RowSpan="1" VerticalAlignment="Stretch"> -->
        <!--                         <UniformGrid x:Name="headerPanel" IsItemsHost="True" Rows="1"/> -->
        <!--                     </Border> -->
        <!--                     <Border x:Name="contentPanel" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.Column="0" Grid.Row="1" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local"> -->
        <!--                         <ContentPresenter x:Name="PART_SelectedContentHost" Content="SelectedContent" Margin="{TemplateBinding Padding}"/> -->
        <!--                     </Border> -->
        <!--                 </Grid> -->
        <!--                 ~1~ <ControlTemplate.Triggers> @1@ -->
        <!--                 ~1~     <Trigger Property="TabStripPlacement" Value="Bottom"> @1@ -->
        <!--                 ~1~         <Setter Property="Grid.Row" TargetName="contentPanel" Value="0"/> @1@ -->
        <!--                 ~1~         <Setter Property="Height" TargetName="RowDefinition0" Value="*"/> @1@ -->
        <!--                 ~1~         <Setter Property="Height" TargetName="RowDefinition1" Value="Auto"/> @1@ -->
        <!--                 ~1~     </Trigger> @1@ -->
        <!--                 ~1~     <Trigger Property="TabStripPlacement" Value="Left"> @1@ -->
        <!--                 ~1~         <Setter Property="Grid.Row" TargetName="contentPanel" Value="0"/> @1@ -->
        <!--                 ~1~         <Setter Property="Grid.Column" TargetName="contentPanel" Value="1"/> @1@ -->
        <!--                 ~1~         <Setter Property="Width" TargetName="ColumnDefinition0" Value="Auto"/> @1@ -->
        <!--                 ~1~         <Setter Property="Width" TargetName="ColumnDefinition1" Value="*"/> @1@ -->
        <!--                 ~1~         <Setter Property="Height" TargetName="RowDefinition0" Value="*"/> @1@ -->
        <!--                 ~1~         <Setter Property="Height" TargetName="RowDefinition1" Value="0"/> @1@ -->
        <!--                 ~1~     </Trigger> @1@ -->
        <!--                 ~1~     <Trigger Property="TabStripPlacement" Value="Right"> @1@ -->
        <!--                 ~1~         <Setter Property="Grid.Row" TargetName="contentPanel" Value="0"/> @1@ -->
        <!--                 ~1~         <Setter Property="Grid.Column" TargetName="contentPanel" Value="0"/> @1@ -->
        <!--                 ~1~         <Setter Property="Width" TargetName="ColumnDefinition0" Value="*"/> @1@ -->
        <!--                 ~1~         <Setter Property="Width" TargetName="ColumnDefinition1" Value="Auto"/> @1@ -->
        <!--                 ~1~         <Setter Property="Height" TargetName="RowDefinition0" Value="*"/> @1@ -->
        <!--                 ~1~         <Setter Property="Height" TargetName="RowDefinition1" Value="0"/> @1@ -->
        <!--                 ~1~     </Trigger> @1@ -->
        <!--                 ~1~ </ControlTemplate.Triggers> @1@ -->
        <!--             </ControlTemplate> -->
        <!--         </Setter.Value> -->
        <!--     </Setter> -->
        <!-- </Style> -->
        <!-- ~1~ <Style x:Key="FocusVisual"> @1@ -->
        <!-- ~1~     <Setter Property="Control.Template"> @1@ -->
        <!-- ~1~         <Setter.Value> @1@ -->
        <!-- ~1~             <ControlTemplate> @1@ -->
        <!-- ~1~                 <Rectangle Margin="2" StrokeThickness="1" StrokeDashArray="1 2"/> @1@ -->
        <!-- ~1~             </ControlTemplate> @1@ -->
        <!-- ~1~         </Setter.Value> @1@ -->
        <!-- ~1~     </Setter> @1@ -->
        <!-- ~1~ </Style> @1@ -->
        <!-- <Style x:Key="MainCenterTabItemStyle" Selector="TabItem" Parent="{x:Type TabItem}"> -->
        <!--     ~1~ <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/> @1@ -->
        <!--     <Setter Property="Margin" Value="0"/> -->
        <!--     <Setter Property="Padding" Value="6,2,6,2"/> -->
        <!--     <Setter Property="HorizontalContentAlignment" Value="Stretch"/> -->
        <!--     <Setter Property="VerticalContentAlignment" Value="Stretch"/> -->
        <!--     <Setter Property="Template"> -->
        <!--         <Setter.Value> -->
        <!--             <ControlTemplate TargetType="{x:Type TabItem}"> -->
        <!--                 <Grid x:Name="templateRoot"> -->
        <!--                     <Border x:Name="mainBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0" Background="{TemplateBinding Background}" Margin="0"> -->
        <!--                         <Border x:Name="innerBorder" BorderThickness="1,1,1,0" Margin="-1" Opacity="0"/> -->
        <!--                     </Border> -->
        <!--                     <ContentPresenter x:Name="contentPresenter" Content="Header" Focusable="False" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" HorizontalAlignment="Center"/> -->
        <!--                 </Grid> -->
        <!--             </ControlTemplate> -->
        <!--         </Setter.Value> -->
        <!--     </Setter> -->
        <!-- </Style> -->
    </Window.Styles>
    <Window.Styles>
        <Style Selector="TextBlock">
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Styles>
    <Window.Resources>
        <SolidColorBrush x:Key="ListBorder" Color="#828790"/>
    </Window.Resources>
    <Grid x:Name="MainWindowGrid" RowDefinitions="Auto,*">
        <Menu Template="{DynamicResource MainWindowMenuTemplate}" x:Name="MainMenu" UseLayoutRounding="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <MenuItem x:Name="ProjectSubMenu" Header="Project">
                <MenuItem Click="CreateProject_OnClick" Header="Create Project..."/>
                <MenuItem x:Name="OpenProjectItem" Command="{Binding OpenProject}" Header="Open Project..." InputGesture="Ctrl+O"/>
                <MenuItem x:Name="OpenRecentSubMenu" Header="Open Recent" ItemsSource="{Binding RecentlyOpened}" IsEnabled="{Binding HasRecents}"/>
                <MenuItem x:Name="SaveProjectItem" Command="{Binding SaveProject}" Header="Save all" InputGesture="Ctrl+Shift+S" IsEnabled="{Binding ProjectOpened}"/>
                <MenuItem Command="{Binding CloseProject}" Header="Close Project" IsEnabled="{Binding ProjectOpened}"/>
            </MenuItem>
            <MenuItem x:Name="BuildSubMenu" Header="Build" IsEnabled="{Binding ProjectOpened}">
                <MenuItem Command="{Binding BuildPs2}" Header="Full Build PS2 ISO" ToolTip.Tip="Packs all assets and chunks then creates PS2 archives then creates the ISO"/>
                <MenuItem Command="{Binding BuildPs2Iso}" Header="Build Only PS2 ISO" ToolTip.Tip="Creates PS2 archives then creates the ISO"/>
                <MenuItem x:Name="BuildXbox" Header="Build XBox ISO" IsEnabled="False"/>
            </MenuItem>
            <MenuItem x:Name="ToolsSubMenu" Header="Tools" IsEnabled="False">
                <MenuItem/>
            </MenuItem>
            <MenuItem Click="Preferences_OnClick" Header="Preferences"/>
            <MenuItem Click="About_OnClick" Header="About"/>
        </Menu>
        <Grid x:Name="MainControlsGrid" Grid.Row="1" ColumnDefinitions="200*,5,50*">
            <Grid x:Name="MainCenterGrid" RowDefinitions="100*,5,25*">
                <ContentControl Content="{Binding ActiveItem}"/>
                <GridSplitter Margin="0" ResizeDirection="Rows" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="5" Grid.Row="1"/>
                <TabControl x:Name="LogTabs" Grid.Row="2">
                    <TabItem Header="Log">
                        <Grid>
                            <avaloniaEdit:TextEditor x:Name="LogText" HorizontalScrollBarVisibility="Disabled" WordWrap="True" VerticalScrollBarVisibility="Auto" ShowLineNumbers="True" Watermark="Logs will be written here..." IsReadOnly="True"/>
                            <gif:GifImage Source="{Binding SadEasterEgg, FallbackValue={x:Null}}" Stretch="Uniform" HorizontalAlignment="Right" Margin="10" IsVisible="{Binding SadEasterEggVisibility}"/>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
            <controls1:GroupBox Header="Project tree" Grid.Column="2">
                <Grid RowDefinitions="54,470*">
                    <TextBox
                        ScrollViewer.VerticalScrollBarVisibility="Disabled"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        Watermark="Search"
                        x:Name="SearchBar" 
                        TextWrapping="NoWrap" 
                        Text="{Binding SearchAsset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLines="1" Margin="0,14,0,15" Height="25"/>
                    <TreeView 
                    Grid.Row="1"
                    x:Name="ProjectTree" 
                    ItemsSource="{Binding ProjectTree, Mode=OneWay}" 
                    HorizontalAlignment="Stretch">
                        <TreeView.ItemTemplate>
                            <TreeDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Background="Transparent" HorizontalAlignment="Stretch" Margin="0,2,0,2" Orientation="Horizontal">
                                    <StackPanel.ContextMenu>
                                        <ContextMenu ItemsSource="{Binding MenuOptions}">
                                            <Interaction.Behaviors>
                                                <EventTriggerBehavior EventName="Opened">
                                                    <InvokeCommandAction Command="{Binding CreateContextMenuAction}"/>
                                                </EventTriggerBehavior>
                                            </Interaction.Behaviors>
                                        </ContextMenu>
                                    </StackPanel.ContextMenu>
                                    <Interaction.Behaviors>
                                        <EventTriggerBehavior EventName="DoubleTapped">
                                            <InvokeCommandAction Command="{Binding CreateEditor}"/>
                                        </EventTriggerBehavior>
                                        <!-- <EventTrigger EventName="MouseMove"> -->
                                        <!--     <cal:ActionMessage MethodName="AssetBlockMouseMove"> -->
                                        <!--         <cal:ActionMessage.Parameters> -->
                                        <!--             <cal:Parameter Value="{Binding ElementName=ProjectTree, Mode=OneWay}"/> -->
                                        <!--             <cal:Parameter Value="$eventArgs"/> -->
                                        <!--         </cal:ActionMessage.Parameters> -->
                                        <!--     </cal:ActionMessage> -->
                                        <!-- </EventTrigger> -->
                                    </Interaction.Behaviors>
                                    <Image Source="{Binding IconPath}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="24" Height="24" Margin="0,0,3,0" Stretch="Uniform"/>
                                    <TextBlock x:Name="AssetBlock" IsVisible="{Binding IsNotRenaming}" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Alias}">
                                        <Interaction.Behaviors>
                                            <DataTriggerBehavior Binding="{Binding IsEnabled}" Value="False">
                                                <ChangePropertyAction PropertyName="Foreground" Value="Gray"/>
                                            </DataTriggerBehavior>
                                            <DataTriggerBehavior Binding="{Binding IsEnabled}" Value="True">
                                                <ChangePropertyAction PropertyName="Foreground" Value="White"/>
                                            </DataTriggerBehavior>
                                        </Interaction.Behaviors>
                                    </TextBlock>
                                    <TextBox x:Name="RenamingTextBox" Text="{Binding NewAlias, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Stretch" IsVisible="{Binding IsRenaming}">
                                        <Interaction.Behaviors>
                                            <EventTriggerBehavior EventName="LostFocus">
                                                <InvokeCommandAction Command="{Binding StopRenaming}"/>
                                            </EventTriggerBehavior>
                                            <EventTrigger EventName="KeyDown">
                                                <InvokeCommandAction Command="{Binding SaveRenaming}" PassEventArgsToCommand="True"/>
                                            </EventTrigger>
                                        </Interaction.Behaviors>
                                    </TextBox>
                                </StackPanel>
                            </TreeDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </controls1:GroupBox>
            <GridSplitter Margin="0" ResizeDirection="Columns" HorizontalAlignment="Stretch" Grid.Column="1"/>
        </Grid>
    </Grid>
</Window>
