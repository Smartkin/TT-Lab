<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:TT_Lab.ViewModels"
             xmlns:cm="http://caliburnmicro.com"
             d:DataContext="{d:DesignInstance Type=viewmodels:EditorsViewModel, IsDesignTimeCreatable=True}"
             x:DataType="viewmodels:EditorsViewModel"
             cm:Bind.AtDesignTime="True"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="TT_Lab.Views.EditorsView">
    <!-- <UserControl.Styles> -->
    <!--     <Style Selector="TabControl"> -->
    <!--         <Setter Property="Padding" Value="2"/> -->
    <!--         <Setter Property="HorizontalContentAlignment" Value="Center"/> -->
    <!--         <Setter Property="VerticalContentAlignment" Value="Center"/> -->
    <!--         <Setter Property="Template"> -->
    <!--             <Setter.Value> -->
    <!--                 <ControlTemplate> -->
    <!--                     <Grid x:Name="templateRoot" ClipToBounds="true" KeyboardNavigation.TabNavigation="Local" -->
    <!--                           ColumnDefinitions="*,0" RowDefinitions="Auto,*"> -->
    <!--                         <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" Height="NaN" Grid.RowSpan="1" VerticalAlignment="Stretch"> -->
    <!--                             <UniformGrid x:Name="headerPanel" Rows="1"/> -->
    <!--                         </Border> -->
    <!--                         <Border x:Name="contentPanel" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.Column="0" Grid.Row="1" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local"> -->
    <!--                             <ContentPresenter x:Name="PART_SelectedContentHost" Content="SelectedContent" Margin="{TemplateBinding Padding}" /> -->
    <!--                         </Border> -->
    <!--                     </Grid> -->
    <!--                     ~1~ <ControlTemplate.Triggers> @1@ -->
    <!--                     ~1~     <Trigger Property="TabStripPlacement" Value="Bottom"> @1@ -->
    <!--                     ~1~         <Setter Property="Grid.Row" TargetName="contentPanel" Value="0"/> @1@ -->
    <!--                     ~1~         <Setter Property="Height" TargetName="RowDefinition0" Value="*"/> @1@ -->
    <!--                     ~1~         <Setter Property="Height" TargetName="RowDefinition1" Value="Auto"/> @1@ -->
    <!--                     ~1~     </Trigger> @1@ -->
    <!--                     ~1~     <Trigger Property="TabStripPlacement" Value="Left"> @1@ -->
    <!--                     ~1~         <Setter Property="Grid.Row" TargetName="contentPanel" Value="0"/> @1@ -->
    <!--                     ~1~         <Setter Property="Grid.Column" TargetName="contentPanel" Value="1"/> @1@ -->
    <!--                     ~1~         <Setter Property="Width" TargetName="ColumnDefinition0" Value="Auto"/> @1@ -->
    <!--                     ~1~         <Setter Property="Width" TargetName="ColumnDefinition1" Value="*"/> @1@ -->
    <!--                     ~1~         <Setter Property="Height" TargetName="RowDefinition0" Value="*"/> @1@ -->
    <!--                     ~1~         <Setter Property="Height" TargetName="RowDefinition1" Value="0"/> @1@ -->
    <!--                     ~1~     </Trigger> @1@ -->
    <!--                     ~1~     <Trigger Property="TabStripPlacement" Value="Right"> @1@ -->
    <!--                     ~1~         <Setter Property="Grid.Row" TargetName="contentPanel" Value="0"/> @1@ -->
    <!--                     ~1~         <Setter Property="Grid.Column" TargetName="contentPanel" Value="0"/> @1@ -->
    <!--                     ~1~         <Setter Property="Width" TargetName="ColumnDefinition0" Value="*"/> @1@ -->
    <!--                     ~1~         <Setter Property="Width" TargetName="ColumnDefinition1" Value="Auto"/> @1@ -->
    <!--                     ~1~         <Setter Property="Height" TargetName="RowDefinition0" Value="*"/> @1@ -->
    <!--                     ~1~         <Setter Property="Height" TargetName="RowDefinition1" Value="0"/> @1@ -->
    <!--                     ~1~     </Trigger> @1@ -->
    <!--                     ~1~ </ControlTemplate.Triggers> @1@ -->
    <!--                 </ControlTemplate> -->
    <!--             </Setter.Value> -->
    <!--         </Setter> -->
    <!--     </Style> -->
    <!--     ~1~ <Style x:Key="FocusVisual"> @1@ -->
    <!--     ~1~     <Setter Property="Control.Template"> @1@ -->
    <!--     ~1~         <Setter.Value> @1@ -->
    <!--     ~1~             <ControlTemplate> @1@ -->
    <!--     ~1~                 <Rectangle Margin="2" StrokeThickness="1" StrokeDashArray="1 2"/> @1@ -->
    <!--     ~1~             </ControlTemplate> @1@ -->
    <!--     ~1~         </Setter.Value> @1@ -->
    <!--     ~1~     </Setter> @1@ -->
    <!--     ~1~ </Style> @1@ -->
    <!--     <Style Selector="TabItem"> -->
    <!--         ~1~ <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/> @1@ -->
    <!--         <Setter Property="Margin" Value="0"/> -->
    <!--         <Setter Property="Padding" Value="6,2,6,2"/> -->
    <!--         <Setter Property="HorizontalContentAlignment" Value="Stretch"/> -->
    <!--         <Setter Property="VerticalContentAlignment" Value="Stretch"/> -->
    <!--         <Setter Property="Template"> -->
    <!--             <Setter.Value> -->
    <!--                 <ControlTemplate TargetType="{x:Type TabItem}"> -->
    <!--                     <Grid x:Name="templateRoot"> -->
    <!--                         <Border x:Name="mainBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0" Background="{TemplateBinding Background}" Margin="0"> -->
    <!--                             <Border x:Name="innerBorder" BorderThickness="1,1,1,0" Margin="-1" Opacity="0"/> -->
    <!--                         </Border> -->
    <!--                         <ContentPresenter x:Name="contentPresenter" Content="Header" Focusable="False" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" HorizontalAlignment="Center"/> -->
    <!--                     </Grid> -->
    <!--                 </ControlTemplate> -->
    <!--             </Setter.Value> -->
    <!--         </Setter> -->
    <!--     </Style> -->
    <!-- </UserControl.Styles> -->
    <Grid>
        <TabControl ItemsSource="{Binding Items}" SelectedItem="{Binding ActiveItem}" Margin="0" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" UseLayoutRounding="False">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel ScrollViewer.VerticalScrollBarVisibility="Disabled" Orientation="Horizontal">
                        <TextBlock Text="{Binding DisplayName, DataType=viewmodels:EditorsViewModel}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </Grid>
</UserControl>
